# Normalize line endings to LF in the repository (checked out per-platform).
* text=auto eol=lf

# Binary assets — never diff or convert.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.woff2 binary
*.ico binary
